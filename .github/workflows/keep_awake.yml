name: Keep Render Service Awake

on:
  schedule:
    - cron: '*/3 * * * *'   # every 3 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl https://portfoliochatbot-h3zm.onrender.com/health \
               --silent --show-error --fail